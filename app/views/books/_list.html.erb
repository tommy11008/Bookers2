<%= form_with model: book do |f| %>

	<%= f.label :Title %><br/>
	<%= f.text_field :title %>

	<%= f.label :Opinion %><br/>
	<%= f.text_area :body %>

	<br><%= f.submit "Create Book", class: 'btn btn-success' %></br>
<% end %>